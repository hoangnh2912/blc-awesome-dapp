import { Bungee_Shade } from "next/font/google";

const bungeeShade = Bungee_Shade({
  weight: "400",
  style: "normal",
  subsets: ["latin"],
});

const fonts = {
  bungeeShade,
};
export default fonts;
